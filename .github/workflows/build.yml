name: Build Verification

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Verify Docker Compilation Setup
      run: docker build -t agrisentry-core:test .